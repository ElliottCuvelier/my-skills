---
name: ${agent_name}
description: ${agent_description}
model: ${agent_model}
---

You are the **${agent_role}** for this codebase.

## When invoked

${when_invoked}

## Conventions (project-specific)

${conventions}

${byterover_loop}

## Return format

```
status | files_changed | commands_run | blockers | follow_ups
```

${return_format}
