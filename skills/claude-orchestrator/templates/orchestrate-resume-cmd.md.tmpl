Resume an interrupted plan execution. **You — this current session — are the orchestrator. Dispatch each step to sub-agents yourself; do NOT spawn a separate orchestrator agent, and do NOT edit files yourself during orchestration.**

Plan path: $ARGUMENTS

1. Locate the installed skill directory (`.claude/skills/claude-orchestrator/`, else `~/.claude/skills/claude-orchestrator/`) and read its `references/ORCHESTRATION.md` — the orchestration playbook.
2. Read the plan file at the given path.
3. Find the first todo with `status: pending` or `status: in_progress`.
4. Show the user a summary:
   - Todos already `completed`: list them with brief outcomes
   - Next todo to execute: its id and content
   - Remaining `pending` todos: count
5. Ask the user: **continue from here** / **abort** / **restart from an earlier todo** (if so, which id?)
6. If continuing: follow the playbook's dispatch loop (Step 3 onward) starting from the first pending/in_progress todo.
7. Return a final summary as usual.

If no todos are pending or in_progress (all completed or failed), tell the user the plan is already finished and show the final state.
