Execute the saved plan at the path provided. **You — this current session — are the orchestrator. Dispatch each step to sub-agents yourself; do NOT spawn a separate orchestrator agent, and do NOT edit files yourself during orchestration.**

Plan path: $ARGUMENTS

1. Locate the installed skill directory (`.claude/skills/claude-orchestrator/`, else `~/.claude/skills/claude-orchestrator/`) and read its `references/ORCHESTRATION.md` — the orchestration playbook.
2. Follow the playbook: load the roster from the install marker, read the plan, parse its `execution:` block (ask the user if it's missing), dispatch each `pending` todo to the right `impl-*` / project / `verifier` / `memory-curator` sub-agent, update todo statuses in the plan file as you proceed, and return a final summary with results, verifier outcomes, and any pending ByteRover review taskIds.

If the path is not provided or is invalid, ask the user to specify the full plan path (typically under `~/.claude/plans/`).
